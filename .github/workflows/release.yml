name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - goos: linux
            goarch: amd64
            suffix: linux-amd64
          - goos: linux
            goarch: arm64
            suffix: linux-arm64
          - goos: windows
            goarch: amd64
            suffix: windows-amd64.exe
          - goos: android
            goarch: arm64
            suffix: android-arm64

    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.21'

      - name: Build
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          CGO_ENABLED: 0
        run: |
          VERSION=${GITHUB_REF#refs/tags/}
          go build -ldflags "-X main.version=${VERSION} -s -w" -o tonnet-proxy-${{ matrix.suffix }} ./cmd/

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: tonnet-proxy-${{ matrix.suffix }}
          path: tonnet-proxy-${{ matrix.suffix }}

  build-macos:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.21'

      - name: Build universal binary
        run: |
          VERSION=${GITHUB_REF#refs/tags/}
          LDFLAGS="-X main.version=${VERSION} -s -w"

          # Build for both architectures
          CGO_ENABLED=0 GOOS=darwin GOARCH=amd64 go build -ldflags "${LDFLAGS}" -o tonnet-proxy-darwin-amd64 ./cmd/
          CGO_ENABLED=0 GOOS=darwin GOARCH=arm64 go build -ldflags "${LDFLAGS}" -o tonnet-proxy-darwin-arm64 ./cmd/

          # Create universal binary
          lipo -create -output tonnet-proxy-darwin-universal tonnet-proxy-darwin-amd64 tonnet-proxy-darwin-arm64

          # Verify
          file tonnet-proxy-darwin-universal

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: tonnet-proxy-darwin-universal
          path: tonnet-proxy-darwin-universal

  release:
    needs: [build, build-macos]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Prepare release files
        run: |
          mkdir -p release
          find artifacts -type f -exec mv {} release/ \;
          ls -la release/

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: release/*
          generate_release_notes: true
